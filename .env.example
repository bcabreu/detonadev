NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Tracking & Analytics (Injetados via Layout nativo no DataLayer com LGPD check)
# Para GTM, insira abaixo. O GA4 e o Meta Pixel DEVEM ser inicializados por dentro do cofre do GTM!
NEXT_PUBLIC_GTM_ID="GTM-XXXXXXX"

# === Automação de Indexação (Sitemap via Google Search Console API) ===
# ⚠️ Não exponha no código fonte. Cole no GitHub Secrets do projeto!
# Secret Name: GCP_SERVICE_ACCOUNT
# Como obter: Crie uma Service Account no GCP, baixe o JSON raiz e marque o Email dela como "Proprietário" no Webmasters (GSC).
